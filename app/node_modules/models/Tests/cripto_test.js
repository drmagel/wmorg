#!/bin/env node

var crypto = require('crypto');

var generate_sessID = function() {
    var sha = crypto.createHash('sha256');
    sha.update(Math.random().toString());
    return sha.digest('hex');
};

console.log(generate_sessID());
