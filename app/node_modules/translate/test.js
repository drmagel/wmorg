#!/bin/env node

var arg = process.argv[2];
var lang = process.argv[3];
var trans = require('translate');
//trans.init('heb');
lang ? trans.init(lang) : trans.init('rus');

if(arg){return console.log(trans.tr(arg))};

